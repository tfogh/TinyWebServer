# TinyWebServer 项目总结

## 项目定位

`TinyWebServer` 是一个基于 **Linux + C++** 实现的轻量级高并发 Web 服务器，面向网络编程与服务端基础架构实践。项目围绕 **epoll 非阻塞 I/O、线程池、HTTP 报文解析、定时器连接管理、日志系统、MySQL 登录注册** 等核心能力展开，具备完整的请求接入、解析、处理与响应链路。

## 技术栈

- **语言/平台**：C++、Linux
- **网络模型**：Socket、epoll、非阻塞 I/O
- **并发模型**：Reactor / 模拟 Proactor、pthread 线程池
- **协议处理**：HTTP/1.1、GET / POST、状态机解析
- **存储与业务**：MySQL、数据库连接池、用户注册/登录
- **系统能力**：mmap、writev、socketpair、alarm、信号处理
- **工程能力**：同步/异步日志、定时器清理、命令行参数配置

## 一句话项目介绍（适合放简历）

基于 Linux C++ 实现轻量级高并发 Web 服务器，采用 **epoll + 非阻塞 Socket + 线程池** 构建请求处理框架，支持 **Reactor/Proactor 两种并发模型、LT/ET 触发模式切换、HTTP 状态机解析、静态资源访问、MySQL 用户注册登录、同步/异步日志**，并通过 Webbench 压测实现 **万级并发连接**。

## 简历版项目描述（推荐直接使用）

### 版本一：偏通用

负责/实现一个基于 **Linux C++** 的轻量级高并发 Web 服务器，整体采用 **epoll + 非阻塞 I/O + 线程池** 架构，支持 **Reactor / Proactor 并发模型切换** 与 **LT / ET 事件触发模式组合**。使用 **状态机** 完成 HTTP 请求解析，支持 **GET/POST**、静态页面及图片/视频资源访问；通过 **MySQL 连接池** 实现用户注册登录；引入 **定时器 + 信号处理** 回收非活跃连接，并实现 **同步/异步日志系统** 提升可观测性与吞吐能力。

### 版本二：偏性能亮点

基于 Linux 网络编程实现高性能 Web 服务器，围绕 **I/O 多路复用、线程池、连接超时管理、数据库连接复用** 等关键模块完成架构设计与编码。项目支持 **HTTP 报文解析、静态资源返回、登录注册 CGI 逻辑、日志异步落盘**，并通过 Webbench 在不同事件触发与并发模型配置下完成压测，具备 **万级并发连接处理能力**。

## 技术要点拆解（适合面试展开）

### 1. 高并发网络模型

- 使用 **epoll** 统一管理监听 socket、连接 socket 和信号管道事件。
- 所有连接均采用 **非阻塞 Socket**，避免单连接阻塞主线程。
- 支持 **LT / ET** 两种触发模式，并允许监听 fd 与连接 fd 自由组合。
- 通过 `EPOLLONESHOT` 避免同一连接被多个工作线程重复处理。

### 2. Reactor / Proactor 双模型设计

- **Reactor**：主线程负责事件分发，工作线程负责读写与业务处理。
- **模拟 Proactor**：主线程先完成数据读取，再将业务处理任务投递到线程池。
- 通过命令行参数即可切换并发模型，便于对比不同模型下的吞吐表现与实现差异。

### 3. 半同步 / 半反应堆线程池

- 基于 `pthread`、互斥锁、信号量实现线程池与任务队列。
- 主线程负责 I/O 事件分发，工作线程负责请求解析与响应构造。
- 通过任务队列削峰，提升大量并发连接下的处理稳定性。

### 4. HTTP 状态机解析

- 使用主从状态机解析 HTTP 请求报文，拆分为：
  - 请求行解析
  - 请求头解析
  - 消息体解析
- 支持 **GET / POST** 请求，处理 `Connection`、`Content-Length`、`Host` 等关键头部。
- 根据 URL 路由静态资源或登录注册逻辑，并生成标准 HTTP 响应。

### 5. 静态资源高效传输

- 使用 `mmap` 将目标文件映射到内存，减少用户态和内核态之间的数据拷贝。
- 使用 `writev` 聚合发送响应头和响应体，降低系统调用次数。
- 支持 HTML 页面、图片、视频等静态资源访问。

### 6. MySQL 连接池与登录注册

- 封装 **数据库连接池**，预创建多条 MySQL 连接供业务线程复用。
- 使用 **RAII** 管理数据库连接获取与归还，降低资源泄漏风险。
- 实现基于表数据的用户注册、登录校验等基础 Web 业务逻辑。

### 7. 定时器与异常连接清理

- 基于升序双向链表实现定时器容器，维护连接超时事件。
- 配合 `alarm + SIGALRM + socketpair`，将信号事件转为统一的 epoll 可读事件处理。
- 对长时间无数据交互的连接进行自动关闭，提升连接资源利用率。

### 8. 同步 / 异步日志系统

- 支持同步日志与基于阻塞队列的异步日志两种模式。
- 日志按日期与行数滚动切分，避免单文件过大。
- 记录服务运行状态、异常信息与请求处理信息，提升问题排查效率。

### 9. 工程化可配置能力

- 支持通过命令行动态配置：
  - 端口号
  - 日志模式
  - LT/ET 触发模式
  - 数据库连接池大小
  - 线程池线程数
  - 优雅关闭
  - Reactor / Proactor 模型
- 便于在开发、压测、演示等不同场景下快速调整服务器行为。

## 可量化亮点

- 项目 README 给出的 Webbench 压测结果显示，在不同配置组合下可达到 **8 万 ~ 9 万+ QPS**。
- 支持 **10500 并发连接** 压测场景，并完成全部请求处理。
- 具备完整的“连接接入 -> HTTP 解析 -> 业务处理 -> 响应发送 -> 超时回收”服务端主流程。

## 可提炼成简历亮点的关键词

- **epoll**
- **非阻塞 I/O**
- **Reactor / Proactor**
- **线程池**
- **HTTP 状态机**
- **MySQL 连接池**
- **mmap / writev**
- **定时器**
- **异步日志**
- **高并发**

## 面试时可重点讲的 4 个点

1. 为什么要同时支持 **LT / ET** 和 **Reactor / Proactor**，它们各自的差异是什么。
2. HTTP 请求是如何通过 **状态机** 逐步解析出来的。
3. 为什么静态文件传输要使用 **mmap + writev**，相比普通读写有什么优势。
4. 如何通过 **定时器 + 信号 + epoll** 清理空闲连接，并避免大量死连接占用 fd 资源。

## 最终推荐写法（适合直接粘贴到简历）

基于 Linux/C++ 实现轻量级高并发 Web 服务器，采用 epoll + 非阻塞 I/O + 线程池，支持 Reactor / Proactor 处理模式和 LT / ET 触发组合。通过 HTTP 状态机解析 GET/POST 请求，提供静态资源服务，并基于 MySQL 连接池实现注册登录；使用 mmap + writev 优化响应，结合定时器+信号+epoll 自动回收空闲连接，附带异步日志系统。

## 核心能力

- 并发模型：支持 Reactor / Proactor 处理模式，兼容 LT / ET
- HTTP 功能：支持 GET/POST，返回静态页面、图片、视频
- 性能优化：使用 mmap + writev 提升文件传输效率
- 数据库：MySQL 连接池支持注册登录
- 连接管理：定时器 + 信号 + epoll 自动回收空闲连接
- 日志：内置异步日志记录系统

## 亮点总结

- 面向高并发场景的轻量 Web 服务器实现
- 支持命令行配置端口、日志、触发模式、线程数、连接池大小等
- 可通过 Webbench 验证万级并发连接处理能力
- 具备完整的“接入 -> 解析 -> 处理 -> 响应 -> 回收”服务端流程

## 简历表述

基于 Linux C++ 实现轻量级高并发 Web 服务器，采用 **epoll + 非阻塞 Socket + 线程池** 架构，支持 **Reactor / Proactor** 模型和 **LT / ET** 触发模式。通过 **HTTP 状态机** 处理请求，支持静态文件服务与 MySQL 用户注册登录，并结合 **mmap + writev** 优化响应性能，配套 **定时器超时回收** 与 **同步/异步日志**。